# Repo do Jarbas: rastrear SÓ o sistema de mentores.
# Ignora tudo no topo e re-inclui só o que é do Jarbas — assim qualquer
# outra skill futura nesta pasta NÃO entra neste repo por acidente.
/*
!/.gitignore
!/README.md
!/_jarbas/
!/jarbas-*/
!/forge-*/
# Projeto "AI Onboarding" (mentor Osmar) — artefatos versionados e sincronizados.
!/ai-onboarding/
# Slash commands do Osmar (precisam morar em .claude/commands para o Claude Code achar).
!/.claude/
.claude/settings.local.json
