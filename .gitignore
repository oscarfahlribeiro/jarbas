# Repo do Jarbas: rastrear SÓ o sistema de mentores.
# Ignora tudo no topo e re-inclui só o que é do Jarbas — assim qualquer
# outra skill futura nesta pasta NÃO entra neste repo por acidente.
/*
!/.gitignore
!/README.md
!/_jarbas/
!/jarbas-*/
!/forge-*/
# Exceção deliberada (2026-07-15): /area é o runtime dos gestores de área do casei
# (não é mentor, mas este repo é o canal de distribuição de skills p/ o sócio).
!/area/
